{shared_opening_block}

{shared_runtime_context_block}

This call is AGENT MODE CONTINUATION / REPLAN planning, not a fresh plan from zero.
Use continuation context, preserve completed progress, and safely re-optimize only the remaining route.

This replan call means the system has already decided that the current mission state warrants a replan.
You should therefore focus on:
- preserving already completed progress,
- avoiding unnecessary repetition,
- adapting the remaining route to current risk and geometry,
- and using continuation evidence rather than restarting from scratch.

Continuation context:
- previous plan: {previous_plan}
- execution history: {execution_history}
- mission progress: {mission_progress}
- completed checkpoints: {completed_checkpoints}
- remaining checkpoints: {remaining_checkpoints}
- current target checkpoint: {current_target_checkpoint}
- replan runtime history:
{replan_history_block}

Agent replan requirements:
- The predicted collision probability refers to the collision risk over the next 3 seconds, not only the current instant.
- Because replan has already been triggered, first determine what preventive avoidance behavior is needed from the predicted collision probability and the current UAV-worker geometry.
- Choose avoidance amplitude according to severity.
- If the predicted collision probability is only mildly elevated and geometry is not severely blocked, a small preventive detour may be enough.
- If the predicted collision probability is clearly high or the geometry is obviously unsafe, use a larger and more conservative detour.
- Do not begin a replan with direct gc(...) when the next-3-second risk is already elevated.
- After avoidance, continue only the remaining checkpoints; never redo completed checkpoints.
- Remaining checkpoint order may be re-sorted dynamically according to safety, geometry, and efficiency.
- The same ml/mr/mf/mb maps to different world directions under different headings; keep this heading-aware in replan.
- Replan should solve the current risk problem while still keeping mission efficiency in mind.

Guides:
{guides}

Agent replan examples:
{typefly_replan_examples}

Task description:
{task_description}

response:
